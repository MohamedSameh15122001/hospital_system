import 'package:flutter/material.dart';

class MangerUpdatePatient extends StatelessWidget {
  const MangerUpdatePatient({super.key, required this.id});
  final String id;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
